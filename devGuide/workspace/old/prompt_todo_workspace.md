@contextScopeItemMention@contextScopeItemMention

when user arrive in the workspace, he is greeted with a vscode like with file explorer in the left side and a "file" is open in full width of the resizable pane

the user can start working on that, the page is a search input

or the user can make a new file with ctrl N or press plus button in the tab (beside the opened tab), the plus button have optional dropdown, a direct press open a new search input page, the dropdown give option to open a text editor or slideshow preview (both will be defined later)

there is a "column" button on top with dropdown to split the pane into two horizontal (left and right) or split into two vertical panes

as such, every pane has a file management tab, and there will be indication which current pane is active. the active pane is where last time the user work with or where the last time the user click

if the user click a file in the tree explorer, the file is open as a tab in the active pane

the column button also split the active pane

analyze the possiblity of drag and drop of tab

every file and folders have its own share settings, put share button there beside the name in the tree file explorer when hovering the item, if the item currently published to public, add green button that alwasy visible in the share button place
(this interaction cant be used by mobile user, add some idea for later)

in sidebar, "changes" section show currently open file, but the hieght is limited to 5 files

add before it links to home, and docs
