# Production Server Setup & Resource Analysis Report

Generated on: **2026-05-23 07:57:00 UTC+7**
Remote Host: `docker-openbayan-fe` (`100.101.207.74` via Tailscale)

---

## 1. System Overview & Operating System
* **Hostname:** `docker-openbayan-fe`
* **Operating System:** Debian GNU/Linux trixie (testing branch)
* **Kernel Version:** `6.17.2-1-pve #1 SMP PREEMPT_DYNAMIC PMX 6.17.2-1 (2025-10-21T11:55Z) x86_64`
* **Virtualization Platform:** Proxmox VE (LXC Container or lightweight VM)
* **Status:** Operational & Clean Install

---

## 2. Resource Allocation & Utilization

### A. CPU Allocation
* **Processor Model:** `AMD Ryzen 5 2600 Six-Core Processor`
* **Cores Assigned:** `6` CPU cores visible

### B. Memory (RAM) Usage
* **Total Memory:** `4.0 GiB`
* **Used Memory:** `101 MiB`
* **Free Memory:** `3.5 GiB` (available memory: `3.9 GiB`)
* **Swap Space:** `512 MiB` (0 B used)

### C. Storage & Disk Usage
* **Total Partition Size (`/`):** `14.0 GiB`
* **Used Space:** `1.3 GiB` (10%)
* **Available Space:** `12.0 GiB`

> [!NOTE]
> The disk space was successfully increased by `10 GB` to a total of `14.0 GiB` (with `12.0 GiB` available). This is fully sufficient for Next.js and Docker builds, though regular system pruning is still advised to keep space clear.

---

## 3. Network Configuration & Open Ports

* **Tailscale IPv4:** `100.101.207.74`
* **Local LAN IPv4:** `192.168.100.46`

### Active Listening Connections (ss -tulpn)
| Protocol | Port | Service / Process | Scope / Bind Address |
|---|---|---|---|
| **TCP** | `22` | SSH Daemon (`sshd`) | `*` (Any interface) |
| **TCP** | `25` | Postfix Master | `127.0.0.1` & `[::1]` (Loopback only) |
| **TCP** | `46038` | Tailscale Agent | `100.101.207.74` |
| **UDP** | `41641` | Tailscale Wireguard Daemon | `*` (Any interface) |
| **UDP** | `68` | DHCP Client (`dhclient`) | `*` (Any interface) |

---

## 4. Software Environment

### A. Core Software Status
* **Python:** `Python 3.13.5` (installed at `/usr/bin/python3`)
* **Docker Engine:** `29.5.2` (running via `systemd`)
* **Container Runtime:** `containerd v2.2.4`
* **Node.js / Bun / PM2:** **Not installed**
* **Nginx / Caddy:** **Not installed**

### B. Active Systemd Services
* `docker.service` (Active & Running)
* `tailscaled.service` (Active & Running)
* `postfix.service` (Active & Running)
* `ssh.service` (Active & Running)

---

## 5. Current Deployment State

* **Active Docker Containers:** None (no running or stopped containers exist).
* **Code / Web Assets:**
  * No deployment directories found in `/root` or `/home`.
  * No `/var/www` web directory present.
  * The server is currently a clean slate.

---

## 6. Installation Log Timeline
According to the local setup trace `/root/.install-28b35eaf.log`:
* **07:42:38 AM:** Host Container OS initialized and updated against Debian trixie sources.
* **07:42:40 AM:** Tailscale setup confirmed; verified IPv4 Internet and DNS paths.
* **07:45:23 AM:** Docker engine suite (`docker-v29.5.2` / `containerd`) successfully provisioned.
* **07:46:08 AM:** Setup and cleanup finished with `[OK]` status.

---

## 7. Recommendations for OpenBayan Frontend Deployment

1. **Leverage Docker:** Since Docker is already fully installed and active, we should build a Docker image for the OpenBayan frontend and run it as a container. This avoids the need to manually install Node.js/Bun or PM2 on the host system.
2. **Setup Reverse Proxy:** We should run a lightweight reverse proxy (such as a Caddy container or Nginx container) on port `80`/`443` or bind the Next.js container directly to the required port on the host to expose the application.
3. **Maintain Disk Space:** Although the disk has been expanded to `14.0 GiB`, using multi-stage Docker builds is still recommended to keep image sizes small and deployment times fast.
4. **Production Domain Mapping:** The public production URL is configured to `https://openbayan.insanmustaqbal.or.id`. This domain should point via Tailscale (or public reverse proxy) to port `3000` on this host.
